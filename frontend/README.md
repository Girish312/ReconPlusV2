## Available Scripts
Check out live website [https://recon-plus.vercel.app/]

In the project directory, you can run:

### `npm start`

Runs the app in the development mode.

### `npm install`

To install all the dependencies listed in a package.json file when setting up the project on a new machine.